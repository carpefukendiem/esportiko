import { media } from "@/lib/data/media";

/** Hero art for /apparel “Browse by category” cards (local WebP). */
export const CATEGORY_CARD_IMAGE_BY_SLUG: Record<string, string> = {
  "t-shirts": media.hero.apparelStack,
  polos: media.portfolio.polo,
  "hoodies-sweatshirts": media.portfolio.hoodieDark,
  hats: media.portfolio.hat,
  jerseys: media.portfolio.jerseyBack,
};
